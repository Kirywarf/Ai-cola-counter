<UserControl x:Class="CalorieCounter.Views.FoodDatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBox Width="220" Text="{Binding SearchText}"/>
            <ComboBox Width="180" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}"/>
            <Button Content="Поиск" Command="{Binding SearchCommand}"/>
            <Button Content="Новый" Command="{Binding NewCommand}"/>
            <Button Content="Сохранить" Command="{Binding SaveCommand}"/>
            <Button Content="Удалить" Command="{Binding DeleteCommand}"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <DataGrid ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" AutoGenerateColumns="False" Margin="4">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Категория" Binding="{Binding Category}"/>
                    <DataGridTextColumn Header="ккал/100г" Binding="{Binding CaloriesPer100g}"/>
                    <DataGridTextColumn Header="Б" Binding="{Binding ProteinPer100g}"/>
                    <DataGridTextColumn Header="Ж" Binding="{Binding FatPer100g}"/>
                    <DataGridTextColumn Header="У" Binding="{Binding CarbsPer100g}"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Grid.Column="1" Margin="4">
                <TextBlock Text="Редактор продукта" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBlock Text="Название"/><TextBox Text="{Binding EditableProduct.Name}"/>
                <TextBlock Text="Категория"/><ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding EditableProduct.Category}"/>
                <TextBlock Text="Калории/100г"/><TextBox Text="{Binding EditableProduct.CaloriesPer100g}"/>
                <TextBlock Text="Белки/100г"/><TextBox Text="{Binding EditableProduct.ProteinPer100g}"/>
                <TextBlock Text="Жиры/100г"/><TextBox Text="{Binding EditableProduct.FatPer100g}"/>
                <TextBlock Text="Углеводы/100г"/><TextBox Text="{Binding EditableProduct.CarbsPer100g}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
