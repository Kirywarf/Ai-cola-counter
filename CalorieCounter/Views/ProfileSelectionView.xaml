<UserControl x:Class="CalorieCounter.Views.ProfileSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Выбор профиля" FontSize="26" FontWeight="Bold" Margin="8"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" AutoGenerateColumns="False" Margin="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Возраст" Binding="{Binding Age}"/>
                <DataGridTextColumn Header="Вес" Binding="{Binding Weight}"/>
                <DataGridTextColumn Header="Цель" Binding="{Binding Goal}"/>
                <DataGridTextColumn Header="Ккал/день" Binding="{Binding DailyCaloriesGoal}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Создать" Command="{Binding CreateCommand}"/>
            <Button Content="Редактировать" Command="{Binding EditCommand}"/>
            <Button Content="Удалить" Command="{Binding DeleteCommand}"/>
            <Button Content="Выбрать" Command="{Binding SelectCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
